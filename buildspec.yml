version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo installing dependencies
      - npm install
  build:
    commands:
      - echo building frontend...
      - npm run build
  post_build:
    commands:
      - aws s3 sync . s3://todo-temp-12342141 --delete
      - aws cloudfront create-invalidation --distribution-id E1MYF0LUTGKODH --paths "/*"

artifacts:
  files:
    - '**/*'
  base-directory: build